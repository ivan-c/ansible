---
dependency:
  name: galaxy
  options:
    role-file: "requirements.yaml"
driver:
  name: docker
lint:
  name: yamllint
  options:
    config-file: ../../.yamllint
platforms:
  - name: deb9
    image: debian:stretch
    groups:
      - vm_hosts
    privileged: True
provisioner:
  name: ansible
  options:
    # Set verbose if `--debug` passed to molecule
    vvv: "{{ lookup('env', 'MOLECULE_DEBUG') | bool }}"
  config_options:
    defaults:
      roles_path: $HOME/.ansible/roles
  lint:
    name: ansible-lint
  inventory:
    group_vars:
      vm_hosts:
        vm_host: "{{ inventory_hostname }}"
        vm_emulation_type: qemu
scenario:
  name: default
verifier:
  name: testinfra
  lint:
    name: flake8
